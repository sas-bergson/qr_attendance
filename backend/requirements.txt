# Flask 2.3.3 and related dependencies for the backend API
# The requirements are pinned to specific versions to ensure stability and reproducibility of the development environment.

Flask==2.3.3
Flask-CORS==4.0.0
Flask-JWT-Extended==4.5.2
psycopg2-binary==2.9.9
python-dotenv==1.0.0
flasgger==0.9.7.1
marshmallow==3.20.1

# Testing dependencies
pytest==7.4.3
pytest-flask==1.3.0
pytest-cov==4.1.0

# Optional: pgAdmin4 for database administration UI
# Install separately if needed: pip install pgadmin4
# Supported versions: 4.22+ (compatible with Flask 2.3.3)